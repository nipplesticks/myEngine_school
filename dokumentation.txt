What do do:

[Magnus]
* Parsing and Rendering an existing model format [X];
* Deferred Rendering [ ]; (Using phong)
* Normal mapping [ ];


float4 normalMapValue = 2.0 * (normalmap.Sample(sampletype, input.tx) - 1.0) - 1.0;
float3 unitNormal = normalize(normalMapValue.rgb);



* Front to Back rendering [ ];
* Glow Effect [ ]

[Victor]
* Height-map terrain rendering, user can walk on the terrain using WASD keys [X];
* "Shadow mapping"/"Dynamic cubic enviroment mapping"/"Dynamic paraboloid mapping" [ ];
* Back face culling using Geometry Shader [ ];
* Mouse Picking [ ]